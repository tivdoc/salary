# Preserved forensic evidence is byte-exact by definition. `V041_MISMATCH_004`
# was lost because git's clean filter normalized the mixed line endings of the
# only surviving copy, and those bytes cannot be reconstructed from the blob.
# These files must never be normalized, diffed as text, or merged.
*.preserved.bin -text -diff -merge
*.recovered.bin -text -diff -merge
docs/release-evidence/sol-comparison-20260910/** -text
docs/release-evidence/sol-canonical-20260910/** -text

# Migration 212 verifies exact installed function definitions from 207/208.
# Preserve those Git blob bytes on Windows as well as Linux; no repo-wide EOL change.
/supabase/migrations/20260914013000_real_service_candidates_claims.sql -text
/supabase/migrations/20260914014000_real_service_provider_requests.sql -text
/supabase/migrations/20260914018000_real_service_canonical_case_planner.sql -text
/supabase/migrations/20260914020000_real_service_candidate_null_guards.sql -text
/supabase/migrations/20260914021000_contract_physical_effective_legacy_period.sql -text
