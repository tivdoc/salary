# Release baseline — P00

- Base SHA: `45abb7e70c1001f88192c983b75008f5f5e198c1`, remote fetched at start; no newer integration commits at that time. PR #1 and both GitHub CI runs are successful.
- Integration branch: `codex/tivdoc-release-completion`; web design branch `codex/website-v1-3` is being edited independently. No changes made in that checkout.
- Node: 22.22.2. Lockfile SHA-256: `cf3377786370b168518b70b958c891bb1196ebb348fb71a3a5c94d3e4c51f895`. Existing `npm ci` install corresponds to that unchanged lockfile.
- Inventory: 108 canonical entries, highest CEP-108, 40 stable dispatcher roots. No new routes introduced in P00/P01. Inventory/closed-runtime/replay tests: 16 passed in three files.
- Database: existing allowlisted DEV project, `tivdoc_v09_devruntime01` for prior runtime evidence; new `tivdoc_release_replay_20260907` for full-chain replay. 75 SQL files: 73 unchanged, two managed-platform compensations recorded verbatim in [replay receipt](release-evidence/P00-migration-replay.json). Expected non-idempotent reapply conflict is recorded, not hidden.
- [Installed comparison](release-evidence/P00-installed-comparison.json): all five upload function definitions and ACLs match. Two upload tables use forced RLS; browser RPC execute is denied; functions are invoker-only. DEV runtime roles have LOGIN by explicit credential provisioning, no superuser/BYPASSRLS/CREATEDB/CREATEROLE. This resolves B-001 without altering roles or weakening assertions about privileges.
- Storage is the existing private DEV `salary-documents` bucket. PostgREST uses a project's default database and cannot be inferred from the named replay DB. Preview proof is tracked separately under P01/B-002. Production credentials and data were not used.
- Read source specifications: complete execution v1.0 and UI/UX v1.2 (curated copies + hashes in `release-specs`), complete v1.1 decisions and UX Run 1 in the local Tivdoc archive, current AGENTS/HANDOFF/upload handoff and relevant development-state sections. Historical model/task instructions are not a new instruction to run Claude or revive superseded units.
- Repeat installed comparison: `node --experimental-strip-types scripts/release-completion/baseline.mts`. Repeat chain replay only in a fresh isolated database with a private credential-file override; never rerun it against the shared DEV database or silently rewrite the shared credential file.
