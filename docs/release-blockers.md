# Release blockers and verification gaps

| ID | Step / environment | Evidence, attempted diagnosis | Completed / remaining / resume condition | Independent continuation |
|---|---|---|---|---|
| B-001 RESOLVED | P00 replay role postconditions / isolated DEV | All 75 files applied (73 unchanged, 2 documented managed-platform compensations). Historical runner expects NOLOGIN for all runtime roles; existing DEV runtime credentials intentionally use LOGIN. No role/password altered to satisfy a test. | Independent catalog comparison verified all four runtime login roles have no elevated attributes; governance owner remains NOLOGIN. Existing dev-credential.mts deliberately provisions login. No role changed. | P01 host-test repairs |
| B-002 | P01 hosted adapter / Preview | Existing upload proof used direct pg on isolated replay plus real Storage. Automatic Vercel preview alone is not PostgREST evidence. | Inspect test deployment env names/target and configure an isolated deployment path or record exact access failure; never redirect to customer DB. | P02 report safety |
| B-003 INTEGRATED / verification pending | P11 parallel UI work | The other checkout now has commit `ac319bf`, based on `2ba7d10`; only generated output/temp files remain untracked. | Read and merge the committed website/branding change into the release branch, preserving the newer upload and report paths. | P09 product contracts |

Missing documents and legal/provider gaps will be added only after inspecting their actual dependencies. Do not treat a historical lack of access as a new proven blocker.

B-002 diagnostics (2026-09-07): Vercel connector returned 404 and Supabase MCP returned permission denied. Authenticated CLI and guarded pg access work. Preview inherited the production Supabase URL; no customer requests were issued. Branch-only DEV URL/keys are being configured. DEV default `postgres` has six historical scaffold tables, zero cases, five old manual migration records, and no upload RPC. The separate 75-migration replay is verified, but cannot prove PostgREST. Reconcile this isolated target or provision an independent Data API target before end-to-end hosted acceptance; do not overwrite historical legal evidence or silently retarget to production. Continue P02 independently.

| B-004 | P03 live OCR/calibration | No OPENAI_API_KEY in isolated DEV credential map, checkout env or Vercel Preview configuration. Existing real-public runner also requires a confirmed rotated key and a ground-truth corpus. | Provider and failure adapters, source integrity, field acceptance, synthetic tests completed. Needs an authorized provider key and calibrated human ground truth; no key/accuracy/attestation invented. Durable invocation continues in P05. | P04 legal research |

| B-005 | P04 legal readiness | Official PDF fetches returned 403/474; archived primary bytes verified, seven visuals read. v15 explicitly has zero human identities/attestations/activation and 36 selected draft parameters, not a live rule catalog. | Research and executable AI worksheets completed. Requires genuine reviewer identities/signatures for source/parameter activation, human-locked goldens and remaining applicability evidence. 2025 pension cap column, schedule exception, historical proration/payout and full period coverage cannot be sold as verified by these 42 arithmetic cases. No human records changed. | P05 orchestration and P08 review workflow |


P06 integration dependencies (not external permission blockers): worker command exists at `scripts/product-workers/request-sweep.mts`; a deployment scheduler still needs wiring. Reminder events are queued intentions, not messages sent. Order-bound SLA persistence belongs to P09; the 2026 service calendar refuses unknown years. Browser validation is deferred to the integrated P11/P13 environment.


B-006 / P07: Resend API key, verified sender domain/DNS, webhook secret, authorized test recipient and delivery proof are not present. Implemented the REST provider, Svix verification, encrypted outbox, controlled worker and durable event inbox. No email sent and no DNS change made. Flags default off; resume with scoped provider configuration and a synthetic/explicitly authorized recipient. Contact-change re-verification workflow is still an implementation dependency, not externally verified.

B-007 / local capacity: C: became full during P07. npm install initially failed with ENOSPC. Automatic approval rejected recursive deletion of the isolated build cache; exact single generated cache files could be removed, and remaining build artifacts were compressed. Installation recovered. P07 build exited successfully but Turbopack reported failure to persist a cache SST due to capacity. User was asked to free at least 2 GB; no other task's files were touched. Further browser/build work needs available disk.

B-007 RESOLVED for continuation: approximately 2.3 GB free after bounded build-cache work and reversible NTFS compression of this worktree dependency files. A subsequent P08 optimized build passed without the prior ENOSPC warning. No other task files deleted.

P08 integration dependencies: the renderer has ToUnicode/ActualText but is not a tagged PDF/UA artifact; screen-reader proof remains open. Correction intake is durable and visible in QA; applying a corrected fact through canonical fact resolution still needs P05 composition. QA sources are downloaded only after operator authentication and checksum validation, but live Storage/browser proof has not yet run for this new route. Provider delivery flags remain off.

P09: Supabase branch discovery was also refused by MCP (`permission denied`). No project or paid branch created. Independent PostgreSQL work remains available. Invoice4U live checkout, receipt and refund have not been verified; the existing official SOAP clearing endpoint and vendor WordPress documentation establish the current integration surface but do not establish a safe refund request schema. No guessed refund API call is implemented. Browser/sales flags stay disabled until provider and delivery capabilities are demonstrated.

B-007 reopened for heavy builds: free space fell to approximately 250 MB during P09; re-compressing this worktree's .next freed no meaningful space. Typecheck and focused tests remain possible; no other checkout files touched.

P10 update: production read-only SQL is available through Supabase MCP; the prior blanket MCP access failure applies to DEV/branch operations, not production SELECT. Four authorized cases inspected, no production mutations. Actual isolated backup/restore succeeded using the existing pinned signed PostgreSQL runtime; unsigned Windows binary invocation was refused by the OS. Current remaining lifecycle work is full case purge/accounting separation and contact re-verification, not a missing restore artifact. Provider delivery/inquiry remains unproven. Two final nonincremental typechecks exhausted system/heap memory; incremental rerun pending.

P10 final verification: incremental `npx tsc --noEmit` passed after the two memory-limited nonincremental attempts. Focused tests (72), lint and diff whitespace checks passed. A fresh optimized build remains unrun due to local disk capacity.

P11/B-007: final tsc and broader Vitest crashed with V8 Zone out-of-memory; two fork workers initially failed to spawn. A smaller four-file/13-test threads run passed. Local dev child exited before listening, Playwright ERR_CONNECTION_REFUSED; closed the release-only browser. No unrelated processes stopped. Existing machine commit headroom is under 1 GB despite about 2 GB free physical memory. Resume heavy checks with disk/virtual memory capacity or an isolated hosted build. P12/SQL work continues.

P12 CI update: GitHub run 34128607354 proves final P11 typecheck/lint remotely. Five complete-suite failures were found (while 2469 passed/48 skipped) and corrected locally with 17 passing checks. Build and closure had been skipped, not passed. P12 commit will trigger the full cloud verification again; this provides a legitimate independent path around local memory for those checks.


B-005 update 7.9.2026: 2025 pension column research is resolved as an explicitly AI-authored section-2 benefits decision in docs/legal/pension-cap-ai-decision-20260907.he.md, based on primary order/statute/table. Active parameter/rule binding, source/accuracy readiness and versioned AI publication are still incomplete; the old owner-recorded resolution history is preserved, and no human attestation is created. P05 actual-worker RLS is now verified by P05-saved-worker-db.json, while operational machine identity provisioning and job/provider completion remain implementation/activation dependencies.
