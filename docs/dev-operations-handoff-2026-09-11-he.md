# חבילת תפעול DEV — נקודת עבודה, 11.9.2026

תחילת החבילה: 06:13:38 UTC; מסירה ועצירה עד 10:13:38 UTC. הבסיס `e52d320` אומת מול HEAD והענף המרוחק, והעץ היה נקי. הענף `codex/tivdoc-release-completion`, PR 2. Production ועץ ה־UI המקביל מחוץ לחבילה.

## נשמר עד כה

- `2501ddd`: מחזור הרשאות חדש ומתועד; הכנה, הפעלה, מצב, השהיה, עצירה וחידוש. הודעות וסמכות חישוב מופעלות בנפרד. מצב קבלות שמורות אינו יוצר SDK ואינו מכיל מפתח OpenAI. 50 בדיקות ממוקדות ועוד 5 בדיקות תצורה עברו; typecheck ו־lint עברו. בדיקת DB ב־rollback הוכיחה שמירת הערכות, דחיית קפיצת גרסה ומניעת ביטול revocation. שתי ריצות CI עדיין פועלות בנקודת הכתיבה.
- `f769ab2`: איגוד הודעות לפי מניפסט סיום שמור ושאלות פתוחות; בדיקה נוספת לפני הפנייה לספק. 27 בדיקות יחידה עברו. הוכחת 14 השאלות מול DB והספק עדיין בעבודה.
- סכמה 157/158 הוחלה ב־DEV המבודד לאחר rollback preflight, ב־06:26 וב־06:35. 157 מאפשרת גרסת הערכה חדשה ושומרת חתימות קודמות; 158 מוסיפה רישום סבבי השלמות ומשתמשת ב־outbox הקיים. ACL ו־RLS נשמרו. ניסיון preflight ראשון של 157 נכשל בשם אילוץ אוטומטי; ההגדרה תוקנה לפני apply. הקבלות ב־`output/release-completion/dev-operations-20260911`.

## עבודה שטרם הוכחה

Webhook: שוחזר מקרה שבו פענוח UTF-8 שינה גוף בקשה לפני אימות חתימה. תוקנו פענוח קשיח, מגבלות גודל ומזהה אירוע, ותיחום סודות; 73 בדיקות ממוקדות עברו. מוכנת פריסת ingress צרה בפרויקט salary הקיים, עם החרגת הגנה לפריסת הכניסה בלבד ו־share מוגבל לפריסת היישום. טרם נקלט Webhook חי בחבילה הזאת. אין מנהרה ואין שינוי Production.

מחזור החיים החדש טרם הופעל במתזמן. התצורה הפרטית מוכנה ב־`../release-work/dev-operations-20260911.private.json`; מזהה החלון `81d7c141-1092-4fbe-b588-ac195dbe951c`, עד 10:05 UTC. ההכנה משאירה שליחה כבויה עד `notifications-resume`. התקציב המקורי נשמר. אישור הניסוי הישן פג ב־03:59:18.791 UTC; נדרשת חתימת בדיקה חדשה, לא עריכת תאריך.

התיק החי השמור הוא `TV-9187C6A6` / `33f41e2f-56b5-420c-8ee2-310201813d35`. גרסת המקור `3281dc36-3c28-47f9-a3eb-1923ff7e2c64`. הריצה הקנונית ההיסטורית `56173811-23c5-4174-8289-2da9d083374a`, רשומת DB `6804fdf2-56e6-4215-86ba-d366236877fb`, ודוח `4cf4d2f6-b3e1-4746-aaa7-58acaaaeeb48`. תוצר העבר לא נוצר בקוד החדש.

14 השאלות והמיילים ההיסטוריים אינם נערכים. לבדיקת איגוד חדשה נבנה תיק QA נפרד דרך ה־runner הרגיל וספק בדיקה מוזרק. זו הוכחת הודעות נפרדת מחילוץ Sol החי; לא תוצג כקריאת ספק חיה. במסירה הסופית יירשמו כל מזהי המקור, הריצה והדוח החדשים.

המסמך יתעדכן במסירה הסופית. [מפת REAL](real-activation-map-2026-09-11-he.md) נשארת בתוקף: מקור, עיגול, תחולה וסמכות אותנטית לא אושרו. [פקודות מחזור החיים](dev-operations-lifecycle-2026-09-11-he.md).
