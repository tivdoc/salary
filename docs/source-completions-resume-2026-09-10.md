# Bounded resume: retained live source to updated engineering report

Owner resumed at 03:54 UTC from `f1ab29e8fc89fa873307c20a1a8dcb52f330f5c3` on `codex/tivdoc-release-completion`; both base CI runs passed. Delivery deadline 07:54 UTC. Production and the separate UI worktree stay untouched.

Implemented source transcriptions for the narrow June2026 engineering case where salary type and base field are both absent and one consistent unknown earnings row exists. Both targets/questions are validated before insertion. Existing candidates remain in P06. Exact checkpoint, uploaded document/version/SHA, month, paid topic, identified answer revision and displayed semantics are bound in SQL and TypeScript. Corrections append history; stale scope, foreign identity and unidentified writes are refused.

Financial wire v2 reconstructs separate engineering facts from those readings and the existing component-nature declaration. It preserves raw OCR, parent canonical facts, legal review gates and v1 report bytes. Source completions and hours are explicitly distinguished from machine extraction in HTML/PDF. Missing document hours are requested as an independent declaration, not a fictitious OCR reading. Pending period answers remain awaiting input regardless of answer order.

DEV schema131→133 passed rollback preflight and actual upgrade; schema134 adds the new hours-question wording and passed its own rollback/upgrade probe. Source-completion requests were opened against retained case `TV-73299C08`, document version `2c9f4382-734a-42ba-9f17-ceb427acc836`. Real DB tests passed exact opening/retry, foreign/unidentified denial, typed original/correction, protected source and revoked/month scope. Test answers rolled back; only the necessary two questions persisted. A temporary test machine was revoked. The previously saved live OCR checkpoint was used without another SDK call; no finding or report was seeded.

Local validation so far: pure source/completion/orchestration tests pass; 50 opener/worker tests pass including preflight pair atomicity and checkpoint-reusing retry. Typecheck and changed-file lint passed before final orchestration edits. Broad suite: 3777 passed, 53 opt-in skipped, 3 failed. Two were intentional schema-tail/definer inventory gates, updated to name all five new reviewed boundaries; their focused rerun plus orchestration passed23. The third is the repository-delivery guard finding ignored local `.env.local`; no secret was detected in Git and the guard remains unchanged. Verify the clean CI checkout separately, rather than removing private runtime configuration or weakening the guard. No second broad local suite is needed absent another concrete risk.

Pending at this code checkpoint: clean-commit typecheck/lint/CI, matching Preview, actual browser answers, managed worker output from those answers, current HTML/PDF/history/foreign/retry proof and actual report-ready delivery/webhook. The old240ILS report remains historical until then. Existing original FAIL evidence is retained. This is still an inactive engineering comparison, not canonical legal activation or readiness for a real customer. DNS/custom sender and canonical activation constraints remain in the previous handoff.
