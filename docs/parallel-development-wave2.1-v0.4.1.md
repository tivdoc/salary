# Parallel Development Wave 2.1 — canonical closure V0.4.1

Wave 2.1 integrates three fail-closed worker changes in the frozen order W1 → W2 → W3. The canonical legal build and retrieval paths now enforce instrument eligibility and source roles, and the controlled-import reader exposes only ledger-bound committed artifacts that pass isolated screening. The checkpoint creates no reviewed or active source, real legal value, rule, Finding, customer result, owner import, migration, external connection, or deployment.

The independent V0.4 verifier accepts the pinned archive structure, manifest, safe extraction, nested evidence, command logs, and Git objects. It also records an unresolved historical evidence defect: eleven V0.4 inventory references across seven tracked paths use worktree-byte hashes that match neither Git blobs nor deterministic CRLF checkout bytes. Wave 2.1 therefore remains `PARALLEL_WAVE_2_1_PARTIAL`; the mismatch is preserved rather than normalized away.

Canonical convalescence content is reduced from 65 container chunks to 11 instrument-bound chunks. Page 25 remains `needs_review`. Secondary explanatory, corroborative, staged, and permit-pending-review sources cannot enter operative candidate sets. The Working Time graph contains 20 nodes and 19 explicitly unverified relations; no consolidated text or commencement is inferred. Seven-topic readiness remains fail-closed.

Controlled import uses an immutable atomic commit marker as the reader authority. All pre-marker states and corrupt journal, event, ledger, or marker records are invisible. Local real-process crash, lock, and reader-race tests pass, but the assurance remains exactly `PARSER_APPLICATION_ISOLATION_VERIFIED` and `PARSER_OS_SANDBOX_NOT_VERIFIED`. Persistent owner imports remain zero.

Run the offline verification and deterministic evidence package with:

```powershell
$env:TIVDOC_LEGAL_NETWORK_DISABLED='1'
npm run parallel:wave21:verify
npm run parallel:wave21:review-package
```

The ignored final archive is `output/parallel-wave-2.1/review-package-v0.4.1.zip`. Its manifest excludes only itself to avoid a recursive hash.
