הוכחת DEV לבקשת מסמך והשלמת מידע — 11 בספטמבר 2026

התוצאה היא **PASS** בבדיקת `saved-review-upload.postgres.test.ts`: ארבע העלאות חתומות בפועל ל־Storage הפרטי, ניתוח דרך ה־worker הרגיל והערכה שמורה של המידע שהתבקש. המקורות הם שלושה קובצי PDF סינתטיים; אחד הועלה פעמיים לבדיקת כפילות. לא נזרעו תוצאת חישוב, דוח או הערכת השלמה.

קבלת [proof.json](proof.json) ואורקל [independent-oracle.json](independent-oracle.json) הועתקו בבתים זהים; [manifest.json](manifest.json) מפרט מקור יחסי, גודל ו־SHA-256. הקבצים נבדקו לפני ההעתקה: אין בהם מסמכי לקוח, פרטי קשר, סודות, כתובות העלאה חתומות או נתיבים פרטיים.

גרסת הקוד שנרשמה: `ce6fedbefc52eb8e91c0eef04148db4672c4fbef`. הקבלה מציינת **`dirty: true`**; אין כאן טענה שההרצה נעשתה מעץ Git נקי. זנב הסכמה הוא `20260911173500_scoped_financial_source_completion.sql` (166), SHA-256: `c8311a60795241a56b54910d14a87e6938d2e38b685d485a6a406d14dd57f5ca`.

תנאי הקבלה שנבדקו בפועל:

| פעולה | תוצאה נדרשת שנצפתה |
| --- | --- |
| העלאת מקור חלקי וניתוח רגיל | נפתחו בקשת מסמך קשורה למקור ושאלה נפרדת על שעות. |
| חודש שגוי או בקשה מתיק זר | לא נשמרה הזמנת העלאה; זהות זרה לא קיבלה את מצב הבקשה. |
| העלאת אותם בתים תחת מזהה גרסה חדש | ההעלאה התקבלה, אך המידע נשאר חסר עקב כפילות. |
| העלאת מקור אחר שעדיין חסר בו חלק מהמידע | לאחר הניתוח נשמר מצב `insufficient`; עצם קבלת הקובץ לא סגרה את הבקשה. |
| כשל יזום לפני שמירת ההערכה | טרנזקציית הניתוח, הדוח וההערכה בוטלה; ההעלאה שכבר הושלמה נשמרה. |
| מקור מלא עם ראיית קריאה חיובית ליעד המדויק | רק בקשת המסמך סומנה `satisfied`, מאותה ריצת דוח. שאלת השעות נשארה פתוחה, ללא שינוי `answered_at` או זיוף תשובה. |
| ניסיון חוזר, שני חיבורי worker במקביל וחיבור מאומת חדש | נשמרו אותם בתים ו־hashes של הדוח וההערכה, ללא כפילות. |
| היסטוריה, הרשאה וגרסת מקור | נשמרו דוחות ישנים; מזהה QA מורשה קיבל את הדוח הנוכחי וההיסטורי, וגישה זרה נדחתה. שינוי גרסת מקור בטרנזקציה שבוטלה שלל זמנית את סיפוק המידע. |

האורקל הוא חשבון המקור הסינתטי בלבד: ברוטו **3,300.00 ₪** פחות ניכויים **198.00 ₪** מול נטו **3,102.00 ₪**, הפרש **0.00 ₪**. אין בכך קביעה על זכאות משפטית או על העברה בפועל לקופה. הדוח האחרון הוא `fdadff01-55e4-41f6-86fb-bbada435e086`, מריצה `0f72bf03-d5fb-4c7a-8887-fc3e6d499f2e`; קבלת ההוכחה כוללת את ה־hashes של ה־HTML וה־PDF ושל הדוחות הקודמים.

גבולות ההוכחה: קריאות המקור הן קריאות AI מפורשות של fixture סינתטי ידוע, שהתקבלו דרך מתאם המקורות המאומת. **לא הורץ OCR חי, לא נבדקה מדיניות `payslip.financial_source` של ארבע קריאות מזוהות ולא בוצעה בדיקת דפדפן.** החיבור החדש מוכיח חידוש חיבור, לא הפעלה מחדש של תהליך חיצוני. המסמכים נוספו כמקורות חדשים; שרשרת החלפת מקור באמצעות בקשה לא נבדקה כאן. שני תיקי ה־QA והיסטורייתם נשמרו, וזהות המכונה הזמנית בוטלה. לא נוצרו sessions ללקוח, לא נשלחו הודעות, לא בוצע פרסום ללקוח ולא שונה Production.

הערת מטא־דאטה: השדה `artifacts[].input_revision` בקבלה המקורית נלקח מ־`command.case_revision`; הוא אינו מונה גרסאות `SourceJob`. הקבלה נשמרה כפי שנוצרה, ואין להשתמש בשדה זה כהוכחה למספר גרסאות הקלט או העבודות בתור.
