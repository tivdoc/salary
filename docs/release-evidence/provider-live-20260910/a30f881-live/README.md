# Actual live-provider evidence, application a30f881

This directory preserves an overall **FAIL**, with independently observed successful boundaries. The initial financial report is historical after source replacement. It uses an inactive DEV comparison rule on synthetic input, plus seven automated identified fixture confirmations. It is not a verified employer debt, a real customer analysis or an activated canonical rule.

- `live-financial-db-proof.json`: original unedited overall FAIL; eight passing DB/Storage boundaries and exact request/run/version provenance.
- `initial-calculated.*`, source PDFs, checkpoints and confirmation files: actual source-run artifacts. Never seed these into another case/version to claim a new live proof.
- `browser-report-proof.json`: actual OTP-authenticated HTML; PDF response HTTP200 but browser attachment ERR_BLOCKED_BY_CLIENT. Local PDF bytes/text/render pass.
- `otp-live-proof.json`: actual OTP receipt/webhooks and browser session evidence. The earlier unconsumed code was invalidated by a later request; consumed_at alone does not prove login.
- `notification-driver-original-FAIL.json`: real Resend completion delivery and an identified UI answer, followed by test adapter/cleanup errors. It remains FAIL.
- `notification-cleanup-recovery.json`: exact owned authority disabled and machine revoked after installing the required tenant context. No RLS/ACL change.
- `completion-browser-proof.json`: actual owner Spam message opened, correct protected link, gross confirmation submitted through UI and preserved after refresh.
- `canonical-audit.json`: actual pre-execution context v4 and seven persisted stage hashes. All legal gates remain not admitted.
- `hebrew-live-proof.json`, `hebrew-quality-review.json`: two actual additional-component Hebrew fixtures fail their independent extraction oracle. No downstream financial proof from these inputs. The earlier raster scan failure remains in `../../provider-restart-20260909/`.
- `provider-budget-ledger.json`: cumulative19 SDK passes; $0.4788 is reserved upper bound, not a cost returned by the provider. Connectivity probe is separate.
- `schema131-contract-proof.json`: upgrade/contract DB evidence with synthetic events, distinct from real signed callbacks.
- `runtime-stop.json`: temporary webhook disabled, relay/tunnel stopped, scheduler Disabled. Credentials remain only in private DEV configuration; no persistent live callback service is claimed.

Application `a30f881dca092bbec91cb48aae7fdfc670d0ee5f`; exact Preview `salary-ivlzv8ge9-tivdoccom-5042s-projects.vercel.app`, deployment `dpl_BhassBYLKav3j3huRTktvTYoeD7g`; isolated DEV schema131. Test-driver commits can be later without an application diff. Final handoff identifies each proof's actual driver SHA. No Production change.
