# Actual retained source-completion delivery

Application d85c2f7 / isolated DEV schema135 / clean integrity driver0749809. [Hebrew handoff](../../source-completions-final-handoff-2026-09-10-he.md).

`calculated.pdf`, `.html` and `.json` are the actual automatically persisted engineering run b4d823c8, not a manually authored report. `source.pdf` was downloaded from immutable Storage and SHA-checked. `proof.json` verifies actual DB/source/read/replay/history bindings; `browser-observations.json` explicitly separates UI observations and the blocked browser PDF download. `notifications.json` contains actual sent/delivered receipts; `visual-review.json` records the separate review of the rendered PDF page. Byte preservation is scoped by this folder's attributes.

Prior product/test FAIL receipts remain. Original live corpus and full-flow failures remain in the previous provider-live evidence directory. No new SDK call or canonical activation is claimed. The independent 100-hour answer is explicitly synthetic. Nine requests remain open; engineering report calculated does not mean case complete. Temporary DEV scheduling/transport is stopped in the included receipts.
