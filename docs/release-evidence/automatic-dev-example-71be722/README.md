# Generated automatic DEV examples

Application71be72242d8af06c04bd7359946efb62e6372f2a; isolated schema127; Preview salary-acl7tou5v-tivdoccom-5042s-projects.vercel.app. All PDFs, HTML and JSON here were generated by separate scheduled worker processes from actual uploaded synthetic Storage files and saved analysis runs. No finding/report was seeded.

`initial-calculated` compares the clear100-hour/3300ILS source. `missing-hours` has no amount. `answered-calculated` follows an identified100-hour answer through the actual Preview UI, producing3540ILS expected,3300ILS recorded and240ILS engineering difference. Run844d1da7-e840-4d62-8ae8-106da67f0c02, input revision31. The source PDF itself has no hours; do not attribute that answer to OCR.

OCR transport, confirmation identities, notification acceptance and paid-order prerequisites are explicitly synthetic. The ordinary canonical report remains a draft with no active findings. This is not a legal debt report, real OCR accuracy result, live OTP login, mailbox delivery, payment proof or Production deployment.

The three `*-fixed-review-1.png` images render every PDF page in the final main run. Visual review found no remaining trace clipping; long identifiers wrap without shortening. `report-390.png`/`report-1440.png` are actual hosted customer screenshots, and the two browser receipts bind their exact deployment. The `replacement` folder is a separate six-check proof of replacing a document while the old crashed worker lease is still unexpired, on the same application/schema.

The first5726684 numerical proof and its PDF clipping failure are preserved in `../automatic-dev-first-5726684`; those PDFs are not the approved visual examples. All owned QA cases, identities and objects in the final proofs were removed; use these saved artifacts rather than expecting a public live case link.
