# Live DEV provider integration — active verification

Resumed by the owner from `61788690388c53bca736506a3ada65fa432c2f9b` at 2026-09-09 20:35 UTC. Original package safe-delivery deadline remains 2026-09-10 00:06 UTC. This supersedes the restart STOP; it does not resume P00–P13. Existing isolated DEV remains schema130. No Production changes.

The browser plugin now works and the existing Resend account is authenticated. A dedicated sending-only API key, testing sender, owner-only allowlist and real seven-event webhook are configured privately for local DEV, worker overlay and only the release branch Preview. There is no verified custom sender domain. The provider's testing sender is limited to its account owner; this is not customer-delivery readiness. A first unused key appeared in a delayed browser accessibility result despite redaction of earlier results; it was immediately revoked, and a replacement captured through fully filtered observations. The active replacement and webhook secret have not been printed or committed.

A temporary Cloudflare Quick Tunnel exposes only a loopback signed-event relay. It has no DB/Storage/OpenAI/sending credentials, refuses inherited secret variables, verifies Svix before forwarding, uses an expiring share for one immutable Preview, bounds body size/concurrency/request count and terminates at its deadline. It does not disable project/deployment protection. The relay is a test service dependent on this workstation, not a persistent deployment. Provider webhook receipt/delivery is not yet proved at this checkpoint. Sources: https://developers.cloudflare.com/pages/how-to/preview-with-cloudflare-tunnel/ and https://vercel.com/docs/deployment-protection/methods-to-bypass-deployment-protection/sharable-links .

Engineering financial source admission now supports one documented paid hourly row, preserving the old separate base/unpaid-detail form. It does not derive missing hours, permit additional paid components or waive uncertainty/period/legal gates. Eleven focused regressions pass, including the two previously failing tests. A new coherent single-row PDF fixture and live-only saved DB harness are prepared, with independent 240 ILS arithmetic expectation, retained original failure receipts and shared 22-pass/$5 ledger. Two inputs were rendered and visually inspected. The harness has not been executed live at this application checkpoint.

Canonical June factual context now loads actual run/stage hashes, authenticated reading journal and extraction receipts in the current scoped worker transaction. Async review diagnostics retain it with the same analysis. Catalog diagnostic fingerprint changes to v3 so old completed runs are not silently reinterpreted. Source/parameter/rounding/legal admission and ordinary executor activation remain separate; no human review was invented. Pure contract41 and recording-SQL loader25 tests pass; actual DB integration remains to be run.

Validation before live proof: focused integration191/191 PASS; full typecheck PASS; changed-file lint PASS. The new DB harness imports successfully with its opt-in disabled (one skip, not DB evidence). Prior real corpus six SDK passes/three failing scenarios remain immutable under `docs/release-evidence/provider-restart-20260909/`; prompt r3 still awaits a live result. New application/Preview/live DB evidence will be appended after verification, never inferred from these local tests.

## 2026-09-09 21:10 UTC — actual provider checkpoint

The owner explicitly disavowed the previously selected Skyview address and authorized tivdoc.com@gmail.com and info@tivdoc.com. Only the Resend account owner's Gmail address is enabled for testing. One earlier send to the unrelated address was rejected HTTP403 by Resend; it was not delivered. The incorrect allowlist was invalidated and replaced in private local/worker and branch Preview configuration. Existing deployed14719d1 still needs replacement to consume this change.

One synthetic connectivity message was accepted by Resend (f8752a6c-2bb4-456e-a616-d3c56bdcf28b), received in the owner's Gmail Spam folder, and independently recorded as real signed email.sent and email.delivered events in DEV. The callback traversed the temporary relay and protected immutable14719d1 Preview. This is real delivery evidence, but not OTP login or completion/report outbox evidence and not proof of inbox placement.

The live saved financial harness actually uploaded bytes, called OpenAI, obtained identified source confirmations and produced an initial engineering report with expected3540/recorded3300/gap240 ILS. Eight checks passed, including retry/restart, foreign access and replacement invalidation. The same canonical parent persisted factual_context_ready with five bound facts and two operands; all legal admissions remain false. The overall harness FAILED at missing-input extraction; QA cases and objects were cleaned. Original failure snapshot and report are retained separately. No complete live flow is claimed.

A local mapper defect that could discard all extracted fields when one optional salary-type pair is malformed was reproduced and repaired with branch-local warnings, preserving uncertainty thresholds. The exact original raw pair is unavailable, so this is a reproduced failure class, not a proven raw-response diagnosis. Fifty-three focused tests, full nonincremental typecheck and changed-file lint pass. CI14719d1 had3578 passing tests and one import-extension guard failure; its four imports are corrected. One explicit attempt2 of the same two synthetic source hashes is prepared, bounded to four additional SDK calls under the original22/$5 ledger; eight earlier reservations remain counted. No attempt2 has run at this checkpoint.

## 2026-09-09 21:36 UTC — CI, real OTP and schema upgrade

Both CI runs34405466590/34405462232 passed for ef03418. Its exact READY Preview is salary-91vq67dat-tivdoccom-5042s-projects.vercel.app / dpl_2rkeKtCaj8FkD6TVNBWNv2EXtiiP, using schema130. An owner-authorized identity fixture (no case, OTP or session seeded) requested a code through the real browser UI. Actual Resend message787d99c8-f2c4-4f8f-bf44-50bd7ca0c08b arrived in the owner's Gmail Spam; the code opened an authenticated session and /cases survived refresh. Reuse of the consumed code was refused by the actual DB verifier. Signed sent/delivered callbacks were persisted by this exact Preview. This direct-send path had omitted the provider ID from its product receipt, so that first proof did not prove product delivery reconciliation.

The new direct Resend receipt RPC preserves provider ID and reconciles actual webhook history before/after receipt creation. It rejects NULL/mismatched receipts and provider IDs already assigned through the outbox; outbox finish has the symmetric collision check and consistent lock order. Old outbox behavior and RPC remain compatible. DDL rollback, upgrade130→131, NULL/foreign/collision/fence/order cases, and two simultaneous web-role clients plus a worker webhook passed in isolated DEV. These migration tests use explicitly synthetic DB events, not signed/live-provider evidence. New schema tail20260909212737_direct_resend_provider_receipts.sql; no Production changes. Full nonincremental typecheck and changed-file lint pass.

Actual attempt2 consumed three calls, bringing the shared ledger to11/$0.2772 reserved. Clear-input report succeeded; missing-input OCR also succeeded, but duplicate gross/deductions/net aggregate rows were represented as wage components and correctly blocked the engineering guard. Overall attempt2 remains FAIL with original artifacts preserved. A narrow mapper now retains such explicit duplicate totals in hashed aggregate observations rather than wages, only when label, exact raw amount, source and high-confidence observations agree; ambiguous/real wage rows are unchanged. Seventeen offline source regressions prove missing_input remains missing until an identified100-hour fixture answer. Hebrew clear/scan old r2 receipts remain failures; no new Hebrew live success is claimed.

One reviewed attempt3 after this concrete aggregate-classification repair is allowed for the same two inputs, under the unchanged22/$5 cap and all11 previous reservations. Optional actual Resend request/report passes use a fresh30-minute capability and one QA owner identity; only generated, current event keys are queued. An optional bounded browser-answer pause lets the actual UI supply the independent synthetic answer. Retaining a real saved QA run after a later failure preserves inspection evidence and still records FAIL; machine authority is disabled in cleanup. This next integrated proof has not run at this checkpoint.

## 2026-09-09 21:57 UTC — real OTP delivery reconciliation and extraction recovery

On clean c5217762/schema131/READY salary-jizphd2ko Preview, a second real browser OTP login and refresh succeeded. Actual Resend message15de2ddd-9083-41ed-89a7-a94c1eadee91 was received in the owner's Gmail Spam. Signed sent/delivered callbacks reached this exact Preview, and the product receipt now stores the provider ID and delivered state. Consumed-code reuse was refused. Safe receipt: release-evidence/provider-live-20260910/otp-real-delivery.json. No session/code was injected and no outbox delivery is inferred from this direct OTP proof.

Actual financial attempt3 FAILED before any financial run: first pass omitted salary_type; targeted recovery read hourly at0.94, but a recovery-only candidate below automatic promotion requirements was not exposed for identified confirmation. The scenario guard correctly refused it; the harness's expected later rejection was also too specific. All two SDK calls are retained:13 total/$0.3276 reserved, zero unknown outcomes. Both QA cases and the uploaded object were removed. Only attempt3's actual files are preserved under output/release-completion/dev-financial-live-flow-c521776-failed; older report/confirmation files in the historical flat directory are not attempt3 evidence. Future harness runs use fresh per-case directories.

Commit6154dca updates migration-tail and definer-count expectations to reviewed schema131 and adds explicit checks of all four declarations; exhaustive search_path/ACL protections remain. Nine focused guards passed. Commitf3b4f2a retains one valid recovery-only observation behind an explicit Gate0 confirmation warning, including at0.99; conflict/invalid/duplicate/failed candidates remain blocked. Resolution policy3 retains compatibility with immutable policy2 checkpoints. Fifty-four focused regressions and changed extraction lint pass. Offline original-checkpoint replay can compute240 ILS only after seven identified synthetic reading confirmations; this is not a new SDK/DB proof.

One reviewed attempt4 is prepared only after this concrete fix, with the original22-pass/$5 ceiling and all13 prior reservations retained. Budget guards19 pass; live DB harness import is one opt-in skip. No attempt4 has run at this checkpoint.

A dedicated devmail.tivdoc.com domain was created in the existing Resend account. DNS is Cloudflare-managed; Domain Connect currently opens Cloudflare Login. No DNS records or root mail configuration were changed. The exact three public DEV verification records are in release-evidence/provider-live-20260910/dev-sender-dns.json. Custom sender remains inactive; owner Gmail alone remains enabled with the testing sender.
