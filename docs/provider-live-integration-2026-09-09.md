# Live DEV provider integration — active verification

Resumed by the owner from `61788690388c53bca736506a3ada65fa432c2f9b` at 2026-09-09 20:35 UTC. Original package safe-delivery deadline remains 2026-09-10 00:06 UTC. This supersedes the restart STOP; it does not resume P00–P13. Existing isolated DEV remains schema130. No Production changes.

The browser plugin now works and the existing Resend account is authenticated. A dedicated sending-only API key, testing sender, owner-only allowlist and real seven-event webhook are configured privately for local DEV, worker overlay and only the release branch Preview. There is no verified custom sender domain. The provider's testing sender is limited to its account owner; this is not customer-delivery readiness. A first unused key appeared in a delayed browser accessibility result despite redaction of earlier results; it was immediately revoked, and a replacement captured through fully filtered observations. The active replacement and webhook secret have not been printed or committed.

A temporary Cloudflare Quick Tunnel exposes only a loopback signed-event relay. It has no DB/Storage/OpenAI/sending credentials, refuses inherited secret variables, verifies Svix before forwarding, uses an expiring share for one immutable Preview, bounds body size/concurrency/request count and terminates at its deadline. It does not disable project/deployment protection. The relay is a test service dependent on this workstation, not a persistent deployment. Provider webhook receipt/delivery is not yet proved at this checkpoint. Sources: https://developers.cloudflare.com/pages/how-to/preview-with-cloudflare-tunnel/ and https://vercel.com/docs/deployment-protection/methods-to-bypass-deployment-protection/sharable-links .

Engineering financial source admission now supports one documented paid hourly row, preserving the old separate base/unpaid-detail form. It does not derive missing hours, permit additional paid components or waive uncertainty/period/legal gates. Eleven focused regressions pass, including the two previously failing tests. A new coherent single-row PDF fixture and live-only saved DB harness are prepared, with independent 240 ILS arithmetic expectation, retained original failure receipts and shared 22-pass/$5 ledger. Two inputs were rendered and visually inspected. The harness has not been executed live at this application checkpoint.

Canonical June factual context now loads actual run/stage hashes, authenticated reading journal and extraction receipts in the current scoped worker transaction. Async review diagnostics retain it with the same analysis. Catalog diagnostic fingerprint changes to v3 so old completed runs are not silently reinterpreted. Source/parameter/rounding/legal admission and ordinary executor activation remain separate; no human review was invented. Pure contract41 and recording-SQL loader25 tests pass; actual DB integration remains to be run.

Validation before live proof: focused integration191/191 PASS; full typecheck PASS; changed-file lint PASS. The new DB harness imports successfully with its opt-in disabled (one skip, not DB evidence). Prior real corpus six SDK passes/three failing scenarios remain immutable under `docs/release-evidence/provider-restart-20260909/`; prompt r3 still awaits a live result. New application/Preview/live DB evidence will be appended after verification, never inferred from these local tests.
