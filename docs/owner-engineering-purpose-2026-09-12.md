# Explicit private owner engineering purpose

The owner's release instruction authorizes a versioned AI activation mechanism, preserving truthful sources, applicability, history and the distinction from human approval. This change supplies a practical **private DEV verification purpose**; it does not decide the separate statutory boundary of the customer service described in `ai-release-product-boundary-2026-09-12.md`.

`tivdoc-owner-engineering-policy-v1` is limited to development, isolated_test, QA, and an exact case, owner identity and immutable enrollment ID. It keeps `human_by_law=unresolved` in the reviewed interpretation and every calculated check. `required` remains blocking. Missing/unknown/conflicting interpretation or source acceptance, incomplete tests, wrong source/build/period, missing case decisions, expiry and revocation still block. No human attestation is issued. The qualified v1 policy cannot parse or publish this purpose.

The ordinary saved source journal, typed answers, case decision recipes, generated catalog and executor are reused. `CaseAnalysisService` saves a separate `owner_engineering` envelope under code version0.6.9; it cannot coexist with `ai_release`. Findings, rule traces, source operands and the report belong to the same run. Aggregate debt and combined amounts remain null. The existing HTML/PDF renderer labels calculated results conditional engineering work and masks blocked financial/nonmonetary outcomes. The historical experimental pipeline is unchanged.

The operator's existing configuration assembly/store/enrollment commands accept the explicit new configuration and assembly schemas. The purpose-bound enrollment ID is chosen before sealing the policy; retry uses that ID and the original request key. Revocation creates a new event. Replaying an old grant after revocation does not renew it; a new grant requires a new scoped policy/configuration/event. Machine epochs and provider ledgers remain independent.

Forward migration `20260912174500_owner_engineering_purpose.sql` extends immutable configuration storage and binds grants to an existing verified QA owner identity. The existing queue transport profile name remains `qualified_ai_v1`; the verified configuration schema chooses the execution purpose. This name alone is never publication authority. The private recorder takes only saved run/report hashes and copies its findings from the completed analysis. Current owner reports require this exact immutable receipt as well as live source/order/authority currentness. Runtime roles cannot enroll, write configurations or supply replacement findings.

Delivery denial is separate from question currentness: owner engineering cannot enqueue/claim managed report or completion delivery through the purpose-gated event boundary, while readiness and operational status retain factual open-question checks. OTP is unchanged. The qualified publication boundary explicitly rejects owner envelopes/configurations. The protected owner reader additionally checks the exact identity on the saved engineering envelope.

## Evidence and remaining work

- Engine final50 tests passed (policy21/runtime6/ordinary service23), with legacy policy/runtime/service coverage retained. This is synthetic engineering evidence, not client source activation.
- Root focused159 tests had one legacy trust-check ordering failure. Restoring compiled-build authentication before payload parsing fixed it; the subsequent41-test control/currentness/report group passed. Counts overlap and are not summed.
- Global TypeScript and lint of all changed TS/MTS/MJS passed. The independent review additionally found notification/status coupling, currentness before recorder completion, nullable SQL digest comparisons, and a blocked nonmonetary presentation leak. Those were corrected before checkpoint.
- The forward schema was executed against existing isolated DEV schema180 and rolled back. Scope probes cover unknown case, unauthorized RPC roles, zero findings and omitted hash/law-state negatives, absent recorder receipt, bound grant/retry, foreign owner refusal, delivery denial and non-revival after revocation. Synthetic SQL configuration payloads are rollback-only boundary fixtures, not accepted runtime configurations.
- The exact migration bytes (SHA bfe9a16f291b593dfbd58024f185aec8ad6a638384b3037516ea1959bce005b9) were subsequently applied to isolated DEV: schema181. No new real enrollment, scheduled worker execution, provider call, message, customer report or browser proof is claimed for this checkpoint. A complete source-reviewed configuration with real bound test evidence and final build, actual DEV execution and current private artifacts are still required. No Production change.

Pension identified-table support and travel general-order-floor v2 are independent improvements in the same checkpoint. Pension table readings preserve exact cells and do not prove product/basis/remittance. Travel's four additive recipes bring54 methods to58; the original source's section30 amendment chain still needs a currentness determination. The tariff source adapter has typed, separate context/price/inventory decisions; ordinary upload-purpose/request/UI wiring remains code work and is not a legal-approval blocker.
