# מסירת חבילת DEV אוטומטית — 9 בספטמבר 2026

מצב ביניים: הוכחה משולבת אוטומטית עברה ב־`5726684bc18b26db4e18cfebdfb86a5762b2bc5a`; בדיקה חזותית מצאה חיתוך שורת ספק ב־PDF, והתיקון עדיין דורש הוכחה חוזרת. בסיס `70e13b9964ab360185970de1a601bbf843ff9e2a` בענף `codex/tivdoc-release-completion`. התחלה 15:01:52 UTC; מסירה בטוחה לכל המאוחר 19:01:52 UTC. אין שינוי Production.

היקף: הפעלה אוטומטית של התור הקיים, חילוץ חי כאשר ספק זמין, כניסה/הודעות, השלמות, ניתוח ודוח לאותו חודש ונושא. תשלום סינתטי מותר ומסומן. אין הרשאה להפוך מחקר AI לאישור אנושי או כלל ניסוי לכלל פעיל.

בעלות: ראשי — SQL/פריסות/כניסה/הודעות/אינטגרציה; סוכן חילוץ — מתאם ספק ועקבות; סוכן worker — תזמון תור קיים והתאוששות.

## חסמים שנמצאו בתחילת העבודה

- מפתח OCR: סריקה של התהליך, User/Machine, קובצי env רלוונטיים ונתוני Vercel לאותו פרויקט ב־development וב־Preview לא מצאה מפתח פעיל. אין קריאת ספק חי. ראיות מלאות יצורפו לאחר אימות החיבור.
- Vercel Cron מופעל בפריסות Production; הוא אינו מסלול מורשה לחבילת Preview זו. נבחנת הפעלה מנוהלת מקומית ב־Windows Task Scheduler אל DB DEV מבודד בלבד.
- הקטלוג הקנוני כיום ללא כללים פעילים. דוחAI לפיv1.1 אינו דורש הבטחת חתימה מקצועית לכל פרסום; נכונות מקורות, תחולה, פרמטרים והפעלת כלל נשארות דרישות נפרדות.

## יומן ראיות

נקודת החילוץ `eb472ddb80acb28a8acdf6f24fe39284d07f66a4` נשמרה ונדחפה; שתי ריצות CI עברו. נשמרו קבלות ספק אמיתי לעומת מוזרק, hash של המסמך והתוצאה, דגם וגרסה, עמודי מקור, שימוש ועלות כשזמינים. נוספו שבעה מסמכי בדיקה עם תוצאות צפויות עצמאיות, כולל סימולציית צילום/סריקה. לא הופעל ספק OCR חי: מפתח חסר.

נקודת האינטגרציה מוסיפה רישום QA מוגבל, איסוף תור קיים בהרשאת מכונה, תקציב ניסיונות אטומי, נעילה/fence, ממשק תפעולי מקומי, חיבור אותו parent ל־DEV כספי ולטיוטה קנונית חסומה, והודעות על השלמה/דוח מתוך רשומות מקור עדכניות. קישור ההודעה דורש זהות תיק. דוח הניסוי נשאר הנדסי; לא הופעל כלל קנוני ולא נזרעו ממצאים.

מיגרציות 125–127 שודרגו בפועל במסד DEV הקיים `tivdoc_release_replay_20260907`, לאחר rollback ו־ACL probes. הסכמה האחרונה: `20260909155945_automatic_dev_notification_snapshot.sql`. מיגרציה 126 מתקנת regexp שנכשל בבדיקת איסוף ראשונה לפני שנאספה עבודה; 127 קושרת את נמען המטען המוצפן לתמונת הזהות שנבדקה לאחר נעילה. קובצי המיגרציות שכבר הוחלו לא שוכתבו.

אימות עד נקודה זו: 126 בדיקות ממוקדות בגל הראשון; 114 בגל worker/contract נוסף; 23 בדיקות בגל הודעות/Webhook/שרשרת. בדיקת DB הודעות עברה שמונה תנאי קבלה: OTP אמיתי שהונפק בשירות, תור מוצפן, נמען מוגבל, retry, אימות קוד, תפוגה, נעילת ניסיונות וביטול גישה. הספק וה־Webhook החתום מוזרקים, ואין הוכחת תיבת דואר או HTTP webhook חי. כשל ניקוי ראשון היה בבדיקה (חסר tenant context), תוקן והניקוי הושלם ללא שינוי RLS. typecheck עבר; lint עבר ללא שגיאות ועם אזהרה קיימת אחת בקובץ capacity proof.

נמצא ותוקן גם באג תפעולי קיים: outbox סימן `provider_receipt_missing` בכל הצלחת ספק, אפילו עם מזהה הודעה. כעת השגיאה נרשמת רק בהיעדר מזהה. קבלת ספק נשארת נפרדת ממסירה.

מפתחות Resend, sender ו־Webhook secret לא נמצאו בבדיקת process/User/Machine, קובצי env רלוונטיים ונתוני אותו פרויקט Vercel (Development ו־Preview). פרופיל הבעלים זוהה דרך החיבור הקיים בלי לפרסם את כתובתו, אך אין ספק/שולח מאומת לבדיקת מסירה. לא נשלחה הודעה אמיתית ולא פנו ללקוחות.

הוכחת Windows Task Scheduler מול `5726684` נקי וסכמה127 עברה בפועל: 11 תנאי DB/Storage, חמישה תנאי הודעות ו־11 בדיקות דפדפן ב־Preview `salary-341ku32zh-tivdoccom-5042s-projects.vercel.app`. תהליך נעצר לאחר commit של claim; תהליכים אחרים כיבדו את הנעילה והתאוששו לאחר202355ms בלי שינוי שעון. שלושה כשלים לפני שמירת התוצאה הובילו ל־dead letter; retry מורשה יחיד אפשר המשך, בלי חילוץ חוזר. החלפת מסמך, שעות חסרות ותשובה דרך UI יצרו ניתוח חדש; נשמרו היסטוריה ומקורות, וגישה זרה נדחתה. שני תיקים, שתי זהויות ושני קבצים סינתטיים נוקו; הרשאות המכונות בוטלו. הדוחות נוצרו בתהליכים חיצוניים מהקלט שהועלה, ללא זריעת ממצאים.

שתי ריצות CI של `5726684` עברו (34376013260/34376008392). ריצות ביניים נכשלו בספירות inventory שנדרשו להתעדכן עבור שני הנתיבים ו־14 הצהרות SECURITY DEFINER החדשות; לא הוסרו בדיקות או הגנות. בדיקת DB advisors בסכמה127: אפס שגיאות, אזהרה קיימת אחת על pgcrypto ב־public. כל12 הפונקציות הסופיות החדשות נבדקו מול הרשאות ושביל חיפוש בפועל.

בדיקה חזותית של הדוח הראשוני מצאה שורת מזהה ספק ארוכה שנחתכה, אף שבדיקות המספרים/גרסה/hash עברו. התוצרים והכשל נשמרו ב־`release-evidence/automatic-dev-first-5726684`. מתווסף תיקון שבירת שורות מדודה עם opt-in לדוחות החדשים בלבד, וכן בדיקת החלפה בזמן נעילה פעילה. אלה אינם מסומנים כהוכחה לפני ריצה מוצלחת.

תיעוד הפעלה: [worker](automatic-dev-worker.md), [חילוץ](automatic-dev-live-extraction.md), [תחולת שכר מינימום וחסמי הפעלה](automatic-dev-minimum-wage-activation.md). הוכחות המסלול הפיננסי הקודם נשמרו בנפרד ואינן הוכחה להפעלה אוטומטית או OCR/OTP חי.
