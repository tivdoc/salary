# חבילת DEV מתוזמנת — תוצאות המסלול, 11.9.2026

**מסירה ועצירה בטוחה:** גרסת היישום וה־worker הסופית `56e1bb5133f162734cd6cf01bd766a9c220b9fee`, סכמה156, [Preview סופי](https://salary-am5irlzin-tivdoccom-5042s-projects.vercel.app), deployment `dpl_DnYEpda4pbUbo5qXrJFiqGXfLyKj`. שתי ריצות CI `34554252667`, `34554250160` עברו typecheck, lint, יחידות/guards, build ו־Production closure. [קבלת האימות והעצירה](release-evidence/sol-scheduled-20260911/final-verification.json).

בגרסה הסופית הוכחו OTP אמיתי נוסף מתיבת הבעלים, חזרה להיסטוריית הסתירה, דוחHTML200 באותו hash, download אמיתי של PDF באותו hash, ושלוש הכחשות404 ריקות למקור זר/לא קיים ולתיק זר. worker נקי מאותו commit הופעל מחדש במתזמן, ללא חילוץ, ממצאים או הודעות כפולים. המסלול שיצר את הדוח עצמו רץ ב־5065488/סכמה155; לאחריו השתנו רישום ההודעות ונוסח סימון DEV, ונוספו156 ובדיקת שרשרת. אין כאן טענה לקריאת OCR חדשה ב־56e1bb5.

ב־02:27:44UTC נעצרו Task Scheduler, supervisor ו־package; machine בוטל ו־capability/enrollment הושבתו. נשארו תוצאה קנונית אחת ו־15 receipts של הודעות תיק; הרשאות הקריאה האבחונית ל־invocation נסגרו לאחרSTOP כמתוכנן ב־RLS, בלי שינוי הגנה. קובצי התצורה וה־ledger נשמרו. אישור הניסוי בדוח פג ב־03:59:18.791UTC, ולכן הקישור המוגן אינו תחליף ל־PDF השמור המצורף. מעבר פקיעה טבעי של התיק הנוכחי לא נמדד; אין להשלים זאת על סמך בדיקה מוזרקת או ראיה היסטורית.

CI של62d957c נכשל רק ברשימת סיומת המיגרציות שהייתה עדיין23קבצים.56e1bb5 הרחיב אותה ל־25 תוך שימור הרשימה הישנה והוספת155/156;8בדיקות השרשרת והרגרסיה עברו ולא הוסרה בדיקה. נוסח הודעת DEV החדש עבר בדיקות; ההודעה ההיסטורית שהגיעה עם הנוסח הישן לא נשלחה מחדש. צילום המסך בשםunknown מציג את אזור השאלות, לא את התשובה עצמה; הראיה הישירה ל־unknown היא [snapshot הממשק](release-evidence/sol-scheduled-20260911/browser/unknown-answer-snapshot.yml) ויומן התשובות.

המשימה הבאה המומלצת, ורק לאחר הוראה חדשה: כניסת Webhook חתומה ב־Preview וחידוש הרשאה מוגבל שאינו משכתב היסטוריה. תנאי סיום: endpoint Preview צר ומוכח, Resend event אמיתי מקושר להודעת הבעלים, duplicate/late event ללא הכפלה, תוקף/עצירה וחידוש מסומנים באותו DEV. אין צורך בחיפוש מפתחות חוזר. החלטות הפעלתREAL נשארות לפי המפה הנפרדת.

## תוצאה מוכחת ב־02:10 UTC

המסלול בוצע בתיק QA חדש `TV-9187C6A6`: OTP אמיתי שנקרא ב־Gmail → העלאת PDF חדש בדפדפן → Windows Task Scheduler → Sol medium חי → סתירת שעות והשלמה בממשק → תשובת unknown וריצה חסומה → תיקון מזוהה עם בסיס וגרסת תשובה חדשה → אישורי בדיקה מבודדים → המנוע והפרסום הרגילים → דוח, PDF והודעה בתיבת הבעלים. לא נזרעו ממצאים או דוח. התשלום והסמכות סינתטיים; אין הפעלת REAL או הוכחת סליקה.

- יישום/worker שחילץ וחישב: `506548866dc6543a4406135e146af7ff61cb8971`; שתי ריצות CI `34551090319`, `34551086779` עברו. [Preview שנבדק](https://salary-44j74nffg-tivdoccom-5042s-projects.vercel.app), deployment `dpl_3pvBJLcJJ4sBKt6WzY1X2P2U8VTv`.
- סכמה: ההעלאה התחילה ב־154; כשל opener תוקן ב־155 והמסלול החי הושלם בה. 156 משלימה רישום הודעות שכבר התקבלו אצל הספק, ללא שליחה נוספת. הקריאה הסופית בדפדפן אומתה גם ב־156. המספרים הם מספר קובצי המיגרציה, לא טבלת migrations במסד.
- ריצה קנונית `56173811-23c5-4174-8289-2da9d083374a`, projection `4cf4d2f6-b3e1-4746-aaa7-58acaaaeeb48`, גרסת קלט31. [דוח PDF שמור](release-evidence/sol-scheduled-20260911/scheduled-report.pdf), [HTML](release-evidence/sol-scheduled-20260911/scheduled-report.html), [מסמך סינתטי](release-evidence/sol-scheduled-20260911/input.pdf), [עקבת חישוב](release-evidence/sol-scheduled-20260911/report-provenance.json).
- צפוי3540.58, מתועד3300.00, פער מועמד240.58₪. השיטה נשארה `6443.85 × שעות רגילות ÷ 182`, עיגול half_up באגורה הסופית. לא נבחר סכום כדי להשיג PASS; החלופה240.00 והכרעת העיגול נותרו החלטה נפרדת ל־REAL.
- מקור `3281dc36-3c28-47f9-a3eb-1923ff7e2c64`, SHA `9685e7123013cbfa83cb777b1bf9217c236ef9114faa8ee316a925878633ad00`. הספק החזיר100/120 עם מקומות נפרדים. העובדה המקורית נשארה conflicted, ללא ערך נבחר.100 הוא **תשובת בדיקה מזוהה עם בסיס**, לא הכרעת OCR; unknown והתיקון נשמרו בגרסאות1/2.

## מטריצת קבלה

| סעיף | הוכח בחבילה הנוכחית | מגבלה שנותרה |
|---|---|---|
| תוויות המקור ב־PDF המורכב | r6 נכשל שוב בשתי תוויות בלבד; r7 עבר את אותו oracle ללא שינוי מקור/ספים; raw ומיפוי נשמרו | מדידה של מסמך מסוים, לא כיול לכל התלושים או סריקות חדשות |
| שעות סותרות והשלמות | מקור100/120, תצוגת שניהם ומסמך המקור, unknown, תיקון100 עם בסיס, זהות ויומן, ריצות חדשות | תשובות תחולה וסיווג הן הצהרות QA ואישורי הבדיקה אינם סמכות אמת |
| עיבוד אוטומטי והתאוששות | תהליכי supervisor/worker עצמאיים במתזמן, כולל עצירה והפעלה מחדש; tick14 כשל אחרי שמירת קבלת ספק, tick15 חזר עליה ללא SDK נוסף | תלוי במחשב/משתמש/רשת, אינו שירות ענן24/7 |
| דוח מאותו ניתוח | Finding רגיל,240.58, HTML/PDF מאותה ריצה ומקורות; שום ממצא/דוח לא נזרע | אישור ניסוי פג ב־03:59:18.791UTC; אין התחייבות שקישור הניסוי יישאר זמין אחרי התפוגה |
| הורדה וגישה | אירוע download אמיתי ב־Chrome; PDF SHA זהה לשמור; HTML SHA זהה; מקור זר/לא קיים ותיק זר מחזירים404 ריק | בדיקת expiry טבעית חדשה בתיק זה עדיין לא בוצעה; הגנות expiry/replacement מהחבילה הקודמת מסומנות כהיסטוריות |
| הודעות | Resend קיבל OTP,14 הודעות השלמה והודעת דוח; נצפו ב־Gmail של הבעלים, בספאם;15 הודעות תיק שוקפו ללא resend | אין Webhook מסירה חדש. הגעה לספאם אינה הוכחת הגעה ל־Inbox או אירוע delivered אצל הספק |
| REAL | הריצה הרגילה ללא סמכות סירבה עם חסמי readiness; חיובית רק namespace isolated_test | מקורות, עיגול, תחולה וסמכות אותנטית לפי [מפת ההפעלה](real-activation-map-2026-09-11-he.md) |

[מטריצת ביקורת החישוב](release-evidence/sol-scheduled-20260911/calculation-audit.md), [ראיות דפדפן ותיבה](release-evidence/sol-scheduled-20260911/browser-and-mailbox.json), [היסטוריית תשובות וריצות](release-evidence/sol-scheduled-20260911/history.json).

## תקלות שנמצאו והפרדת הראיות

**מוצר שתוקן:** חסר חוזה לשעות בכותרת; ערבוב תיאור ותא אחוז אצל הספק; `target` מקומי ב־PL/pgSQL התנגש בעמודה והחזיר42702 בפתיחת ההשלמה; הודעות מנוהלות לא יצרו receipt במוצר, והאילוץ הישן הרשה רק שלוש תבניות למרות חוזה חמש תבניות.155/156 עברו rollback preflight ואימות DEV. לא הושבתו בדיקות ambiguity או הרשאות. אחרי156 mirror כפול נשאר רשומה יחידה, ניסיונות/מזהי הספק לא השתנו, capability זר נדחה והרשאות record הקיימות לא הורחבו.

**בדיקה/כלי:** בחירת חודש ראשונית לא התעדכנה בגלל syntax שגוי בפקודת אוטומציה; השרת סירב וההעלאה בוטלה בממשק לפני העלאת יוני. סרגל ניווט חסם click על checkbox; גלילה רגילה למרכז פתרה. ה־CSP של HTML הדוח אסר fetch כמצופה; בדיקות API נעשו מעמוד התיק הרגיל. בדיקת migration156 השתמשה תחילה בשם עמודה שגוי ובהנחת ACL שגויה; תוקנה מול הסכמה בפועל, ללא שינוי הרשאות כדי לעבור.

**סביבה:** כניסת Webhook עדיין חסומה. הפעלת מנהרה ציבורית נדחתה בבדיקת אישור אוטומטית עם `blocked by policy`; לא נמסר נימוק מפורט ולא נעשה ניסיון לעקוף. פרויקט ingress נפרד קיים אך הפריסה ההיסטורית היחידה חזרה כ־Production ונמחקה; אין endpoint Preview פעיל ומוכח. נדרש endpoint Preview נפרד לחתימות Resend, עם גישה צרה לפריסה המוגנת, ואז עדכון URL אצל Resend ואימות אירוע אמיתי. אין לבטל את הגנת האתר כולו.

## תקציב

Ledger חדש נפרד:6/12 בקשות תוכן,3 counts ו־3 generations; רזרבה שמרנית2.136/5דולר,0unknown. עלות generation מחושבת0.208136דולר לפי התעריף המוצמד; הספק לא החזיר עלות חשבונית ומחירcount לא אומת. כשלr6, retryr7 והחילוץ מההעלאה נשמרו כולם; ledger ישן לא אופס. [סיכום](release-evidence/sol-scheduled-20260911/budget-summary.json).

## אימות התיקון האחרון

לאחר155/156: typecheck עבר, lint לקבצים ששונו עבר,3 בדיקות רגרסיה לשרשרת155 עברו ו־13 בדיקות הודעות עברו. [בדיקת mirror ב־DEV](release-evidence/sol-scheduled-20260911/mirror-rollback.json) עברה ב־rollback: יצירת mirror לשתי התבניות, retry, capability/תיק/זהות זרים, webhook סינתטי מוקדם/מאוחר/כפול, הפרדת sent/delivered ושחזור מלא. worker אמיתי בדק replay של enqueue קיים וסירוב גישה זרה בלי לשנות outbox. זו אינה קבלת Webhook חיה מ־Resend, ו־fresh enqueue מלא לא הורץ מחדש בבדיקה זו.

נוסח הודעת הדוח הרגיל במסלול DEV תוקן לסימון מפורש של ניסוי סינתטי ולשלילת אישור אדם/חוב מאומת. ההודעה שכבר הגיעה ב־02:00 נשמרת כהיסטוריה עם הנוסח הכללי הישן; לא נשלחה מחדש רק כדי לשנות את הניסוח.

## עצירה וחידוש — בלי איפוס היסטוריה

בחלון ההפעלה המקורי בלבד, pause מתחיל ב־`Disable-ScheduledTask -TaskName 'Tivdoc-AutomaticDev-Proof-20260909'` ובשינוי `enabled=false` בקובץ supervisor הפרטי. יש להמתין לקבלת הסיום של הילד שבבעלות אותו supervisor. אין למחוק locks או unknown reservations. לאחר בדיקת תוקף machine/capability/package/assessment, manifest תואם ו־ledger ללא מצב לא ידוע, אפשר להחזיר enabled=true ואז `Enable-ScheduledTask` ו־`Start-ScheduledTask` לאותו שם משימה. זו הפעלה מתוזמנת חיצונית, לא קריאה ל־worker מתוך בדיקה.

אם build השתנה: `node src/server/product/processing/managed-worker-build.mjs` מתוך עץ נקי, לאחר Preview READY מאותו commit, ואז `node ../release-work/rebind-sol-scheduled-host.mjs`. הפקודה משמרת ledger ומעדכנת pins בלבד; היא אינה מחדשת סמכות או מפעילה שירות. פרטי השליטה נשמרים ב־`../release-work/sol-scheduled-{supervisor,worker-env,package,control}-20260911.private.json`, והמשגר ב־`run-sol-scheduled-supervisor.ps1`.

אחרי STOP שמבטל machine/capability, או אחרי תפוגת חלון החבילה, **אין פקודת renewal מלאה קיימת**. זו תלות טכנית מפורשת לחבילה הבאה: epoch מוגבל חדש שאושר, machine/capability והרשאת QA חדשות, והערכת בדיקה חתומה חדשה הקשורה לקלט העדכני. הקוד מסרב אחרי04:19:48UTC בכוונה. אין להאריך תאריכים בקובץ כדי לעקוף זאת. `prepare` ו־`assess` הנוכחיים הם one-shot ואינם פקודות restart; הסקריפט לתיק retained הישן אינו מתאים לתיק זה. נשמרו ההגדרות, הסודות הפרטיים וה־ledger; אין צורך בחיפוש מפתחות נוסף או ביצירת מפתח הצפנה חוזרת.

## נקודות שמירה קודמות — היסטוריה

בסיס המסירה `3a796edeac21fd513f984663c70fa47078aef1e9` אומת מול הענף המרוחק, ללא שינויים מקומיים בנקודת ההתחלה. הענף `codex/tivdoc-release-completion`, Draft PR2. עץ ה־UI המקביל ו־Production לא שונו. תחילת החבילה00:19:48UTC ב־11.9.2026; מסירה ועצירה עד04:19:48UTC.

נקודת השמירה הראשונה `85932b9` כוללת wrapper מוגבל ל־Sol במתזמן, סירוב להוצאה לא מוגבלת, תיקון foreign-source404 והכנת תיקQA ריק. ההרחבה הנוכחית כוללת prompt/schema r6, שמירת תצפיות מקור, השלמת סתירה מזוהה עם בסיס ו־unknown, source-admission-v2, שמירה רגילה של החישוב ומנגנון supervisor/health מוגבל. אישורי REAL לא נוספו.

סכמה154 הוחלה ב־DEV המבודד `tivdoc_release_replay_20260907`, project `cpzrbidxftzqcfeqqusu`, לאחר rollback preflight לכל מיגרציה קדמית. 149בריאות;150יומן סתירה;151קיבוע תשובה ועובדות אפקטיביות;152טיוטה חלקית;153דרישת קבלת ספק;154סירוב היקף מרובה תלושים ללא ניסיון חילוץ חוזר. מספר הסכמה הוא מספר קובצי השרשרת, לא ledger מהמסד. קבלות, hashes ו־ACL נשמרו ב־`output/release-completion/sol-scheduled-20260911/`.

בנקודת השמירה: typecheck עבר; lint עבר עם אזהרת unused import היסטורית אחת ב־document-capacity-proof, ללא שגיאות. בדיקות ממוקדות של הסוכנים ושל המשלב עברו; הן חופפות ואין לסכום את מספריהן. בדיקת DB השוותה את יעד הסתירה מה־SQL ל־TypeScript על קבלת Sol חיה שמורה מחבילה קודמת, ללא קריאת ספק נוספת. הרשאות raw/write נשארו חסומות לתפקידי runtime. כשלי בדיקות תשתית (env שמצביע ל־DB אחר, עמודת opened_at, undefined באסרט hash, mocks וטיפוסי fixtures) זוהו ותוקנו; הם אינם הוכחת כשל OCR חדשה.

**טרם הוכח בנקודת השמירה:** upload חדש→scheduler→Solr6→השלמה בממשק→דוח→הודעה באותה גרסה, Webhook חדש והורדת דפדפן. אין להציג קוד או replay כהוכחה זו. תיקQA `TV-9187C6A6` הוכן עם תשלום סינתטי בלבד וללא מסמך/ממצא/דוח. ה־ledger החדש נפרד מהקודם, עד12בקשות תוכן כולל count ועד$5 רזרבה, המוקדם; היה ריק בעת ההכנה.

הוראות התזמון/עצירה/חידוש: [runbook](automatic-dev-worker.md). החיבור משתמש ב־Windows Task Scheduler הזמין בחשבון: זו הפעלה אמיתית חיצונית לתהליך האינטראקטיבי, אך היא תלויה במחשב, משתמש ורשת ואינה שירות ענן24/7. תזמון החבילה יוגבל לתיקQA וליעד הבעלים המאומת `tivdoc.com@gmail.com`. בסיום יישמרו התצורה וההוראות, והתזמון וההרשאות הזמניות ייעצרו.

ראו [תיקון החילוץ וההשלמות](sol-hours-conflict-contract-2026-09-11-he.md) ו[מפת הפעלת REAL](real-activation-map-2026-09-11-he.md). מפת ההפעלה מפרידה בין ראיות דין, החלטת עיגול, תחולה, מדיניות reviewers ותפעול. אין חתימה מקצועית מומצאת ואין שינוי היסטורי בשיטת240.58 לעומת240.00.

מסמך זה יעודכן בתוצאות ההוכחה הסופית; הוא אינו מסירת הצלחה של המסלול.

עדכון01:30UTC: `115d7e5` ו־`aa21e32` נדחפו ושתי ריצות CI של כל אחת עברו. Task Scheduler הפעיל תהליכי supervisor/worker נפרדים לאחר סיום פקודת ההפעלה האינטראקטיבית. ב־115d המתאם סירב לפני SDK בגלל enum המוגבל לבדיקה; ב־aa21 נוסף permit פרטי ומוגבל ל־DEV, ונשמרו guard היסטורי, התאמה לתיק/מקור/מפתח, תוקף ותקציב. תהליכי המתזמן העצמאיים חזרו עם exit0, אבל בשלב זה עדיין לא הועלה מסמך לתיק ולכן זו הוכחת תזמון במצב המתנה בלבד.

OTP נשלח בפועל באמצעות Resend ונקרא מתיבת הבעלים ב־Gmail (בתיקיית ספאם). הושלמה כניסה בדפדפן, בלי הזרקת session של Tivdoc, גם ב־Preview שלaa21. `sent` במסד הוא קבלת ספק בלבד; Webhook חדש טרם הוכח. ניסיון להפעיל מנהרה ציבורית לכניסת Webhook נדחה בכלי הביצוע עם `blocked by policy`; לא הופעלה מנהרה בחבילה הזאת ולא נעשה ניסיון לעקוף את הדחייה.

קריאת Solmedium חיה חדשה ל־PDF העברי המורכב ב־aa21/r6 חזרה עם קבלה תקינה, אך נכשלה באותן שתי תוויות OT: הספק צירף את125%/150% לתא התיאור. המקור והאורקל נשארו ללא שינוי; המיפוי לא יצר את השגיאה. שאר בדיקות האורקל הזה עברו. נשמרו raw/mapped/normalized וקבלת ספק, 35.463שניות,4567קלט/3325פלט, עלות דור מחושבת0.084768דולר ללא מחירcount מאומת. התקציב הכולל בשלב זה:2בקשות תוכן,0.712דולר רזרבה,0תוצאות לא ידועות. מעטפת הבדיקה הראשונה סירבה בגללNODE_ENV שירשה מתצורת worker; הדבר תוקן במעטפת הפרטית לפני הקריאה החיה. אין לטעון שהתוויות תוקנו רק משום שנוספה הנחייתr7; נדרשת מדידה חדשה והכישלוןr6 נשמר.
