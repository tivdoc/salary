# חבילת DEV מתוזמנת — נקודת שמירה בתהליך

בסיס המסירה `3a796edeac21fd513f984663c70fa47078aef1e9` אומת מול הענף המרוחק, ללא שינויים מקומיים בנקודת ההתחלה. הענף `codex/tivdoc-release-completion`, Draft PR2. עץ ה־UI המקביל ו־Production לא שונו. תחילת החבילה00:19:48UTC ב־11.9.2026; מסירה ועצירה עד04:19:48UTC.

נקודת השמירה הראשונה `85932b9` כוללת wrapper מוגבל ל־Sol במתזמן, סירוב להוצאה לא מוגבלת, תיקון foreign-source404 והכנת תיקQA ריק. ההרחבה הנוכחית כוללת prompt/schema r6, שמירת תצפיות מקור, השלמת סתירה מזוהה עם בסיס ו־unknown, source-admission-v2, שמירה רגילה של החישוב ומנגנון supervisor/health מוגבל. אישורי REAL לא נוספו.

סכמה154 הוחלה ב־DEV המבודד `tivdoc_release_replay_20260907`, project `cpzrbidxftzqcfeqqusu`, לאחר rollback preflight לכל מיגרציה קדמית. 149בריאות;150יומן סתירה;151קיבוע תשובה ועובדות אפקטיביות;152טיוטה חלקית;153דרישת קבלת ספק;154סירוב היקף מרובה תלושים ללא ניסיון חילוץ חוזר. מספר הסכמה הוא מספר קובצי השרשרת, לא ledger מהמסד. קבלות, hashes ו־ACL נשמרו ב־`output/release-completion/sol-scheduled-20260911/`.

בנקודת השמירה: typecheck עבר; lint עבר עם אזהרת unused import היסטורית אחת ב־document-capacity-proof, ללא שגיאות. בדיקות ממוקדות של הסוכנים ושל המשלב עברו; הן חופפות ואין לסכום את מספריהן. בדיקת DB השוותה את יעד הסתירה מה־SQL ל־TypeScript על קבלת Sol חיה שמורה מחבילה קודמת, ללא קריאת ספק נוספת. הרשאות raw/write נשארו חסומות לתפקידי runtime. כשלי בדיקות תשתית (env שמצביע ל־DB אחר, עמודת opened_at, undefined באסרט hash, mocks וטיפוסי fixtures) זוהו ותוקנו; הם אינם הוכחת כשל OCR חדשה.

**טרם הוכח בנקודת השמירה:** upload חדש→scheduler→Solr6→השלמה בממשק→דוח→הודעה באותה גרסה, Webhook חדש והורדת דפדפן. אין להציג קוד או replay כהוכחה זו. תיקQA `TV-9187C6A6` הוכן עם תשלום סינתטי בלבד וללא מסמך/ממצא/דוח. ה־ledger החדש נפרד מהקודם, עד12בקשות תוכן כולל count ועד$5 רזרבה, המוקדם; היה ריק בעת ההכנה.

הוראות התזמון/עצירה/חידוש: [runbook](automatic-dev-worker.md). החיבור משתמש ב־Windows Task Scheduler הזמין בחשבון: זו הפעלה אמיתית חיצונית לתהליך האינטראקטיבי, אך היא תלויה במחשב, משתמש ורשת ואינה שירות ענן24/7. תזמון החבילה יוגבל לתיקQA וליעד הבעלים המאומת `tivdoc.com@gmail.com`. בסיום יישמרו התצורה וההוראות, והתזמון וההרשאות הזמניות ייעצרו.

ראו [תיקון החילוץ וההשלמות](sol-hours-conflict-contract-2026-09-11-he.md) ו[מפת הפעלת REAL](real-activation-map-2026-09-11-he.md). מפת ההפעלה מפרידה בין ראיות דין, החלטת עיגול, תחולה, מדיניות reviewers ותפעול. אין חתימה מקצועית מומצאת ואין שינוי היסטורי בשיטת240.58 לעומת240.00.

מסמך זה יעודכן בתוצאות ההוכחה הסופית; הוא אינו מסירת הצלחה של המסלול.
