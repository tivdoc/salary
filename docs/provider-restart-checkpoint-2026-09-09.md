# Safe restart checkpoint — 2026-09-09 20:33 UTC

The owner requested a link or safe restart after the connected browser plugin failed. Work is PAUSED for restart, not completed. Base: `588da79fc1b6841efc5962c4a507f141ac4e0286`; branch `codex/tivdoc-release-completion`; Draft PR https://github.com/tivdoc/salary/pull/2. Do not resume P00–P13. Resume only the current bounded live-provider/canonical package after the owner restarts it.

## What changed and what was actually proved

- The owner-specified `.env.local.txt` was read locally. It contains a valid OpenAI definition followed by a non-key duplicate. Only the valid provider key and dated model were imported into gitignored app configuration and the existing private worker overlay. No source file, key, owner destination or encryption value is committed. Existing encryption and owner allowlist were preserved.
- OpenAI model access and a small actual Responses API call succeeded: `gpt-4o-mini-2024-07-18`, HTTP 200, 16 input + 2 output tokens. Its safe receipt is retained. Provider billing cost was not returned.
- The same OpenAI configuration was written as sensitive variables only to the existing salary project's branch-specific Preview environment. No new deployment has received these settings yet. Production was not changed.
- Six actual SDK extraction passes completed for legacy clear, Hebrew clear and Hebrew raster inputs. All three corpus scenarios FAILED validation. Receipts, raw checkpoints and ledger are preserved, not replaced with injected success. Corpus usage: 93,079 input + 2,967 output tokens; budget reservations $0.1512, plus $0.0252 for the separate connectivity probe. Actual charged cost is unknown.
- Observed failures include full-date-range period normalization, missing/misclassified wage rows, deductions/pension column confusion and omitted fields. The new parser accepts only an explicit valid full calendar-month range. Reversed, partial and cross-month ranges remain refused. Versioned prompt r3 clarifies rows versus totals without oracle values. **60/60 focused local tests and offline normalization replay passed. New prompt r3 has not been tested live.** Offline replay is not another provider run.
- The pure canonical factual-context contract and 41 focused tests passed, plus its changed-file lint. It validates saved pins/source bindings and keeps legal activation/publication false. It is not connected to the saved server pipeline yet.
- Root extracted the existing engineering source guard and added a regression for a single paid hourly row. **Current checkpoint intentionally retains two failing new tests (9/11 pass)**: paid-hourly form, and that form with missing hours. This is unfinished work, not a healthy release baseline. The old guard behavior is unchanged; no guard was weakened to make it pass.
- Shared topic rule-input projection was extracted without intentionally changing its hash recipe. Root has not run overall typecheck, lint, build or integration checks on this checkpoint.

## Browser and environment state

The owner reports a Resend connection through the plugin. The plugin fails before browser access with `failed to write kernel assets: The system cannot find the path specified (os error 3)`, including after a reset. The separate headed Playwright session `provider-resend` remains at https://resend.com/login even after navigating to onboarding. This is a browser access/environment blocker, not proof that the owner's account is signed out everywhere. No Resend key/sender/webhook was configured and no message or OTP was sent.

Existing isolated DEV remains schema **130**, tail `20260909185038_june2026_collection_source_link.sql`. No DB migration or new DB financial proof ran in this continuation. Managed task `Tivdoc-AutomaticDev-Proof-20260909` remains Disabled; no automatic live processing is claimed. Prior READY Preview remains application `f0d8081ddf02fd308c14710fefe1218ac1c608f4`, deployment `dpl_HLPwjZyHPupt2VThza9ZV7KAUnSE`, https://salary-ekh8i0mbo-tivdoccom-5042s-projects.vercel.app. It does not include this checkpoint or the newly configured provider settings.

## Exact next work after restart

1. Restore plugin browser access or sign in through the headed Playwright session, then configure the existing Resend account with DEV-only credentials/sender/webhook. Use only the already verified private owner destination. Do not recreate encryption/destination or search for keys again.
2. Complete `dev-financial-source.ts` narrowly: one paid `hourly_base` row matching the normalized base field, or the existing base + one unpaid hourly detail. Preserve confidence, complete-component, warning, document/version and legal guards; missing hours must remain missing. Make the two new regressions pass.
3. Create a new coherent synthetic single-row table fixture and `dev-financial-live-flow.postgres.test.ts`. Neither was written before pause. Use actual immutable upload → SDK → checkpoint → identified source confirmations → calculation → stored HTML/PDF; never consume the old fixture's injected `output`. Independent expected arithmetic remains 100 × 35.40 − 3300 = 240 ILS, explicitly an engineering comparison.
4. Use the existing persistent 22-pass/$5 corpus ledger without reset. Six reservations are already consumed. New two-source saved DB proof would require at most four additional passes, with no automatic SDK retries. Preserve old failed receipts. Versioned prompt retries require an explicit policy-aware reservation design; do not silently reuse a consumed source/pass.
5. Connect the canonical factual context using actual persisted stages and journal-authenticated readings. An initial `saved-june2026-admitted-context.ts` server helper is saved but UNTESTED, UNINTEGRATED and without its planned tests; review SQL joins, runtime authority and journal comparisons before use. Root still needs async `reviewDiagnostics`, a catalog/diagnostic version bump, server loader integration and legacy idempotency tests. Ordinary rule executor admission remains open; do not invent legal review or human confirmation.
6. Commit a clean tested application, then run actual DEV proof and deploy the matching Preview before claiming any same-version hosted result. Live OTP/webhook and report are unproved. No customer debt, full payslip calibration, payment processing or Production readiness is claimed.

Private restart helpers/credentials/receipts are under the sibling `../release-work` directory. Relevant helpers: `run-with-private-provider.mjs`, `import-owner-provider-config.mjs`, `probe-owner-openai.mjs`, `configure-owner-openai-preview.mjs`. Import/probe/config already succeeded; do not rerun them unnecessarily. Set `TIVDOC_DEV_ENV_FILE` to the absolute sibling `release-work/replay.env` for DB proofs; the default points at a different database and must remain refused. Do not print secrets or add private files to Git.

All agents were asked to stop. No live provider call, test, migration or deployment should be started as part of this checkpoint delivery.
