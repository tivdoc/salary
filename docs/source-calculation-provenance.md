# Source calculation provenance — P05/P08, 2026-09-09

Base: f273f81bb2f381a602cf8ffec54dcd0c2881e0a2 on `codex/tivdoc-release-completion`. The integrated customer Preview remains exact 115a2dd; this addition is not deployed.

The legacy calculation input requires a fact UUID and fact path even for a legal parameter. A regression test demonstrates that it refuses a parameter as a parameter but accepts that same number when falsely labelled a fact. Historical traces keep their original schema; the new `tivdoc-source-calculation-trace-v1` preserves distinct fact and parameter sources.

Each trace holds the exact canonical fact snapshot, typed lossless fact projections, full versioned parameter candidates and hashes, RuleSpec package and hash, rule-input and catalog pins, original interpreter steps/output/hashes and a content hash. Validation reconstructs the typed inputs from the saved facts and parameters and re-runs the existing RuleSpec interpreter. It rejects source/value/step substitutions even if an outer hash was recomputed. No duplicate arithmetic engine or floating-point conversion was added. Nested pension amounts and basis-point rates and decimal hours retain their units and exact rational values. Missing or unconfirmed facts do not become zero.

The persistence boundary checks case, analysis run, topic, full fact set, snapshot hash, rule input and catalog against the enclosing bundle. The trace writer independently reads the three persisted canonical stages under the verified tenant/case/run, checks their hashes and source pins, and validates the entire batch before writing. It confirms an insertion or an identical scoped retry instead of silently succeeding when no analysis row matched. Existing PostgreSQL append-only/RLS constraints are reused; no migration is introduced. The separate case-free `private.legal_operations_execution_traces` remains the existing research-execution store.

**Authority is explicitly `arithmetic_provenance_only`.** This is not an operative rule, unpaid gap, high-certainty finding, publication or pricing permission. The synthetic +1 XTS rule used in tests carries no legal approval. The customer report builder rejects this research trace type, and canonical findings remain disabled. The saved worker has not been wired to a real active RuleSpec executor. Those are remaining implementation tasks, not requirements for an invented human signature.

Verification records will distinguish local tests from actual PostgreSQL proof. The opt-in `TIVDOC_SOURCE_TRACE_DB_PROOF=1` extends the existing owned field-confirmation DB test: actual saved worker facts (including customer confirmation), real PostgreSQL trace storage/roundtrip/retry, transaction rollback, foreign tenant refusal and immutable-row refusal. Its trace and analysis are rolled back; its two synthetic cases/identities are cleaned and its machine session revoked. It does not use customer files, provider OCR, browser assertions or production.

Next: compose source traces with explicit expected-versus-paid gap semantics, source/applicability/AI-decision gates and the actual canonical finding/projection/quote reader. An entitlement or subtotal must never be reused as an unpaid gap. Continue the remaining P00–P13 ordinary work; this provenance foundation alone does not complete P05/P08.
